<html xmlns:v="urn:schemas-microsoft-com:vml"
xmlns:o="urn:schemas-microsoft-com:office:office"
xmlns:w="urn:schemas-microsoft-com:office:word"
xmlns:m="http://schemas.microsoft.com/office/2004/12/omml"
xmlns="http://www.w3.org/TR/REC-html40">

<head>
<meta http-equiv=Content-Type content="text/html; charset=utf-8">
<meta name=ProgId content=Word.Document>
<style>
body { font-family: "微软雅黑", "Microsoft YaHei", "SimSun", Arial; font-size: 11pt; line-height: 1.8; color: #333333; }
h1 { font-size: 18pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei; text-align: center;
     border-bottom: 2.25pt solid #2C3E50; padding-bottom: 6pt; }
h2 { font-size: 14pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei;
     border-bottom: 1.0pt solid #BDC3C7; padding-bottom: 3pt; margin-top: 20pt; }
h3 { font-size: 12pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei; margin-top: 16pt; }
code { font-family: Consolas, "Courier New"; background: #f5f5f5; padding: 1px 3px; font-size: 10pt; }
pre { font-family: Consolas, "Courier New"; background: #f8f8f8; padding: 10pt;
      border: 1px solid #ddd; font-size: 10pt; white-space: pre-wrap; line-height: 1.5; }
ul { margin-top: 6pt; margin-bottom: 6pt; }
li { margin-bottom: 3pt; }
.tag1 { display: inline-block; background: #E8F4F8; padding: 2px 8px; border-radius: 3px;
       font-size: 9pt; color: #2C3E50; margin-right: 6pt; }
</style>
</head>
<body>

<h1>并发协作工具笔记</h1>

<h2>一、什么是CountDownLatch</h2>

<p><span class="tag1">定义</span> <b>CountDownLatch = 倒计时门闩</b>，是JUC包下的并发通信工具类，作用是让一个或多个线程等待其他线程完成前置任务后再继续执行。</p>
<p>CountDown是"计数递减"，Latch是"门闩"——门闩没打开前，谁都过不去。</p>
<p>打个比方：CountDownLatch就像<b>运动会的起跑发令枪</b></p>

<ul>
<li>有3名运动员要入场（3个前置任务），裁判在终点等着</li>
<li>每进来一个运动员，计数器减1</li>
<li>3个运动员都到齐了（计数器=0），"砰"一声枪响，裁判宣布比赛开始（等待的线程继续执行）</li>
<li>枪响之后就收不回来了——CountDownLatch只能用一次</li>
</ul>

<h2>二、CountDownLatch核心用法</h2>

<h3>核心方法</h3>
<pre>
CountDownLatch latch = new CountDownLatch(3);  // 构造，设置初始计数

latch.await();             // 阻塞等待，直到计数减为0
latch.await(3, TimeUnit.SECONDS);  // 超时等待，超时后返回false
latch.countDown();         // 计数减1
latch.getCount();          // 获取当前计数值
</pre>

<h3>完整示例：游戏加载资源</h3>
<pre>
public class CountDownLatchDemo {
    static class PreTask implements Runnable {
        private String task;
        private CountDownLatch latch;

        public PreTask(String task, CountDownLatch latch) {
            this.task = task;
            this.latch = latch;
        }

        @Override
        public void run() {
            try {
                Thread.sleep(new Random().nextInt(1000));
                System.out.println(task + " - 加载完成");
                latch.countDown();  // 完成一个，计数减1
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }

    public static void main(String[] args) throws Exception {
        CountDownLatch latch = new CountDownLatch(3);  // 3个前置任务

        // 主线程等待
        new Thread(() -> {
            System.out.println("等待资源加载...");
            latch.await();  // 阻塞，等3个任务都完成
            System.out.println("全部加载完成，游戏开始！");
        }).start();

        // 启动3个加载任务
        new Thread(new PreTask("加载地图", latch)).start();
        new Thread(new PreTask("加载人物", latch)).start();
        new Thread(new PreTask("加载音乐", latch)).start();
    }
}
</pre>

<h3>实现原理</h3>
<p>CountDownLatch内部基于AQS实现，有一个继承了AQS的内部类Sync：</p>
<ul>
<li>构造时传入的count值，被设置到AQS的 <code>state</code> 变量中</li>
<li>调用 <code>countDown()</code> 时，通过CAS把state减1</li>
<li>调用 <code>await()</code> 时，判断state是否为0，不为0就进入AQS等待队列阻塞</li>
<li>当state减到0时，唤醒所有等待的线程</li>
</ul>

<h2>三、什么是生产者消费者模式</h2>

<p><span class="tag1">定义</span> <b>生产者消费者模式</b>是经典的多线程并发协作模式：生产者线程生产数据放入共享区，消费者线程从共享区取数据消费，两者通过共享区解耦。</p>
<p>共享区满了生产者就阻塞等待，共享区空了消费者就阻塞等待。</p>
<p>打个比方：就像<b>快递驿站</b></p>

<ul>
<li>快递员（生产者）把包裹放到驿站货架（共享区）</li>
<li>取件人（消费者）从货架上取包裹</li>
<li>货架满了，快递员就先不送了，等着（生产者阻塞）</li>
<li>货架空了，取件人就等着，有新快递了再取（消费者阻塞）</li>
<li>快递员不关心谁来取，取件人不关心是谁送的——完全解耦</li>
</ul>

<h2>四、三种实现方式</h2>

<h3>方式1：Object的wait/notify</h3>
<p>最原始的实现方式，利用对象监视器锁的等待通知机制。必须在synchronized块中调用。</p>

<pre>
// 标准范式：永远在while循环里wait
synchronized (共享对象) {
    while (条件不满足) {
        共享对象.wait();  // 释放锁，进入等待队列
    }
    // 条件满足，执行业务
}

// 通知方
synchronized (共享对象) {
    改变条件;
    共享对象.notifyAll();  // 唤醒所有等待线程
}
</pre>

<h3>方式2：Lock的Condition</h3>
<p>基于ReentrantLock的Condition，原理和wait/notify一样，只是API不同。</p>

<pre>
Lock lock = new ReentrantLock();
Condition condition = lock.newCondition();

// 等待方
lock.lock();
try {
    while (条件不满足) {
        condition.await();  // 对应wait()
    }
} finally {
    lock.unlock();
}

// 通知方
lock.lock();
try {
    改变条件;
    condition.signalAll();  // 对应notifyAll()
} finally {
    lock.unlock();
}
</pre>

<h3>方式3：BlockingQueue（推荐）</h3>
<p>最简单的实现方式，BlockingQueue自带阻塞的put和take方法，生产者消费者都只需要一行代码。</p>

<pre>
static class Producer implements Runnable {
    private BlockingQueue&lt;Integer&gt; queue;
    public Producer(BlockingQueue&lt;Integer&gt; q) { this.queue = q; }

    @Override
    public void run() {
        while (true) {
            int data = new Random().nextInt();
            queue.put(data);  // 队列满了自动阻塞
        }
    }
}

static class Consumer implements Runnable {
    private BlockingQueue&lt;Integer&gt; queue;
    public Consumer(BlockingQueue&lt;Integer&gt; q) { this.queue = q; }

    @Override
    public void run() {
        while (true) {
            int data = queue.take();  // 队列空了自动阻塞
        }
    }
}

// 入口：5个生产者，10个消费者，队列容量8
BlockingQueue&lt;Integer&gt; queue = new LinkedBlockingQueue&lt;&gt;(8);
ExecutorService pool = Executors.newFixedThreadPool(15);
for (int i = 0; i &lt; 5; i++)  pool.submit(new Producer(queue));
for (int i = 0; i &lt; 10; i++) pool.submit(new Consumer(queue));
</pre>

<p>三种方式对比：BlockingQueue最简洁，实际开发优先用；wait/notify是底层原理，面试要懂；Condition用得较少。</p>

<h2>五、关键技巧与坑点</h2>

<h3>技巧1：CountDownLatch配合实现并发测试</h3>
<p>想让多个线程同时开始执行某个任务，可以用CountDownLatch做"发令枪"：</p>
<pre>
CountDownLatch startLatch = new CountDownLatch(1);  // 发令枪

for (int i = 0; i &lt; 10; i++) {
    new Thread(() -> {
        startLatch.await();  // 所有线程在这里等
        // 真正的任务
    }).start();
}
startLatch.countDown();  // 一声令下，10个线程同时开始
</pre>

<h3>技巧2：生产者消费者模式的四大优点</h3>
<ul>
<li><b>解耦</b>：生产者和消费者互不依赖，改一个不影响另一个</li>
<li><b>复用</b>：生产者类和消费者类可以独立复用</li>
<li><b>削峰</b>：短时间高并发请求可以先放进队列，慢慢消费，不会打垮系统</li>
<li><b>异步</b>：生产者不用等消费者处理完，放进去就返回，响应更快</li>
</ul>

<h3>坑点1：CountDownLatch只能用一次</h3>
<p><b>现象</b>：CountDownLatch减到0后，再调用await()不会再阻塞了。</p>
<p><b>原因</b>：CountDownLatch没有重置计数的方法，一旦归零就失效了。</p>
<p><b>解法</b>：需要重复使用的场景，用CyclicBarrier（可循环使用的屏障）。</p>

<h3>坑点2：countDown一定要放在finally里</h3>
<p><b>现象</b>：程序卡死，所有线程都不动了。</p>
<p><b>原因</b>：某个前置任务抛异常了，没执行到countDown，计数器永远到不了0，等待线程一直阻塞。</p>
<p><b>解法</b>：把countDown放在finally块中，确保一定会执行。</p>

<h3>坑点3：wait必须在while循环里，不能用if</h3>
<p><b>现象</b>：用if判断条件+wait，多线程下偶尔出异常，比如数组越界。</p>
<p><b>原因</b>：线程被唤醒后条件可能已经变了。比如两个消费者，一个消费完了数据，另一个被唤醒后数据已经空了，用if的话不会再判断就直接操作。</p>
<p><b>解法</b>：永远在while循环中调用wait，唤醒后重新检查条件，不满足就继续等。这是黄金法则第一条。</p>

<h3>坑点4：用notifyAll不用notify</h3>
<p><b>现象</b>：多生产者多消费者下，程序突然"假死"，所有线程都在等。</p>
<p><b>原因</b>：notify只随机唤醒一个线程，如果唤醒的是同类线程（比如生产者唤醒生产者），那消费者那边永远没人唤醒，最后大家都等着。</p>
<p><b>解法</b>：用notifyAll而不是notify，用signalAll而不是signal。这是黄金法则第二条。</p>

<h3>坑点5：notify早期通知导致永久阻塞</h3>
<p><b>现象</b>：线程A还没调用wait，线程B已经notify了，导致A之后调用wait时没人唤醒，永远阻塞。</p>
<p><b>原因</b>：notify不留下"通知记录"，如果等待的线程还没开始等，通知就丢失了。</p>
<p><b>解法</b>：配合一个boolean状态变量，notify前改变状态，wait前先判断状态，如果已经满足就不用等了。</p>

<h2>六、总结</h2>

<ul>
<li>CountDownLatch = 倒计时门闩，让一个线程等多个前置任务完成后再执行，只能用一次</li>
<li>CountDownLatch基于AQS实现，state存计数值，countDown减1，await阻塞等待</li>
<li>生产者消费者模式通过共享队列解耦生产者和消费者，是最经典的并发协作模式</li>
<li>三种实现方式：wait/notify（底层）、Condition（Lock版）、BlockingQueue（推荐，最简洁）</li>
<li>生产者消费者模式四大优点：解耦、复用、削峰、异步</li>
<li>两条黄金法则：①永远在while循环中wait；②用notifyAll不用notify</li>
<li>CountDownLatch的countDown一定要放在finally里，确保异常时也能执行</li>
<li>需要可重复使用的屏障，用CyclicBarrier而不是CountDownLatch</li>
</ul>

</body>
</html>
