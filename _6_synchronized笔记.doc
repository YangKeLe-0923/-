<html xmlns:v="urn:schemas-microsoft-com:vml"
xmlns:o="urn:schemas-microsoft-com:office:office"
xmlns:w="urn:schemas-microsoft-com:office:word"
xmlns:m="http://schemas.microsoft.com/office/2004/12/omml"
xmlns="http://www.w3.org/TR/REC-html40">

<head>
<meta http-equiv=Content-Type content="text/html; charset=utf-8">
<meta name=ProgId content=Word.Document>
<style>
body { font-family: "微软雅黑", "Microsoft YaHei", "SimSun", Arial; font-size: 11pt; line-height: 1.8; color: #333333; }
h1 { font-size: 18pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei; text-align: center;
     border-bottom: 2.25pt solid #2C3E50; padding-bottom: 6pt; }
h2 { font-size: 14pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei;
     border-bottom: 1.0pt solid #BDC3C7; padding-bottom: 3pt; margin-top: 20pt; }
h3 { font-size: 12pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei; margin-top: 16pt; }
code { font-family: Consolas, "Courier New"; background: #f5f5f5; padding: 1px 3px; font-size: 10pt; }
pre { font-family: Consolas, "Courier New"; background: #f8f8f8; padding: 10pt;
      border: 1px solid #ddd; font-size: 10pt; white-space: pre-wrap; line-height: 1.5; }
ul { margin-top: 6pt; margin-bottom: 6pt; }
li { margin-bottom: 3pt; }
.tag1 { display: inline-block; background: #E8F4F8; padding: 2px 8px; border-radius: 3px;
       font-size: 9pt; color: #2C3E50; margin-right: 6pt; }
</style>
</head>
<body>

<h1>synchronized核心知识速览</h1>

<h2>一、什么是synchronized</h2>

<p><span class="tag1">定义</span> <b>synchronized</b> 是Java的内置锁关键字，用来保证同一时刻只有一个线程执行被修饰的代码，解决多线程的线程安全问题。</p>
<p>它本质上就是一把<b>"卫生间门锁"</b>——一次只能进一个人，进去后锁门，出来后开门，下一个人才能进。</p>
<p>打个比方：synchronized就像公共卫生间</p>

<ul>
<li>共享资源是卫生间（临界区代码），大家都要用</li>
<li>第一个人进去，锁上门（加锁），其他人只能在外面等（阻塞）</li>
<li>里面的人用完了，开门出来（释放锁），下一个人才能进去</li>
<li>谁先抢到不一定，synchronized是非公平锁，谁跑得快谁先进</li>
</ul>

<h2>二、三种使用方式</h2>

<h3>方式1：修饰实例方法</h3>
<p>锁的是当前对象实例 <code>this</code>，同一个对象的synchronized方法互斥。</p>

<pre>
public class Counter {
    private int count = 0;

    // 锁的是调用这个方法的对象实例
    public synchronized void add() {
        count++;  // 同一时间只有一个线程能执行
    }
}
</pre>

<p><b>注意</b>：两个不同对象的synchronized方法不互斥，因为锁的不是同一把锁。</p>

<h3>方式2：修饰静态方法</h3>
<p>锁的是类的Class对象（<code>Counter.class</code>），类的所有实例共用一把锁。</p>

<pre>
public class Counter {
    private static int count = 0;

    // 锁的是Counter.class对象，全局一把锁
    public static synchronized void add() {
        count++;
    }
}
</pre>

<h3>方式3：修饰代码块</h3>
<p>最灵活的方式，自己指定锁对象，只锁住需要同步的代码，粒度更细。</p>

<pre>
public void doSomething() {
    // 不需要同步的代码...

    synchronized (this) {       // 指定锁对象
        // 需要同步的代码块
        count++;
    }

    // 不需要同步的代码...
}
</pre>

<p><b>推荐</b>：尽量用同步代码块而不是同步方法，锁的范围越小越好，减少阻塞时间。</p>

<h2>三、锁升级原理</h2>

<h3>为什么要有锁升级</h3>
<p>synchronized早期是重量级锁，每次加锁释放锁都要操作系统帮忙，性能很差。</p>
<p>JDK6之后做了优化，引入了<b>锁升级</b>机制——根据竞争情况逐级升级，从无锁→偏向锁→轻量级锁→重量级锁，竞争越激烈，锁级别越高。</p>
<p>打个比方：就像会议室管理</p>

<ul>
<li>偏向锁=你有固定工位，默认就是你的，不用每次申请</li>
<li>轻量级锁=会议室人少，大家CAS抢一下，抢到就用</li>
<li>重量级锁=人太多抢不到，排号等吧，有专人管理</li>
</ul>

<h3>锁对象头</h3>
<p>锁的信息存在对象头（Mark Word）里，不是存在synchronized关键字本身。对象头里记录了当前锁的状态和持有者。</p>
<p>这也是为什么"锁的是对象不是代码"——锁是对象的属性，代码只是申请使用这把锁。</p>

<h3>四种锁状态</h3>

<ul>
<li><b>无锁</b>：没有线程竞争，对象头里存的是对象哈希码等信息</li>
<li><b>偏向锁</b>：只有一个线程用这把锁，把线程ID记在对象头里，下次这个线程来直接用，不用CAS</li>
<li><b>轻量级锁</b>：有少量线程竞争，线程用CAS方式尝试获取锁，抢不到就自旋一会儿（忙等）</li>
<li><b>重量级锁</b>：竞争激烈，自旋多次都抢不到，升级成重量级锁，抢不到的线程挂起（阻塞）</li>
</ul>

<p><b>注意</b>：锁只能升级不能降级（偏向→轻量→重量，不可逆）。除非进入安全点重置偏向状态。</p>

<h2>四、核心特性</h2>

<h3>特性1：可重入</h3>
<p>同一个线程可以多次获取同一把锁，不会自己把自己锁死。锁对象头里有个计数器，加锁一次+1，释放一次-1，减到0才真正释放。</p>

<pre>
public synchronized void methodA() {
    methodB();  // 同一个线程，已经有锁了，可以直接进
}

public synchronized void methodB() {
    // 可重入：不会死锁
}
</pre>

<p>没有可重入的话，上面代码会死锁——methodA拿到锁后调methodB，methodB又要等锁，结果就是自己等自己。</p>

<h3>特性2：非公平</h3>
<p>synchronized默认是非公平锁——后来的线程可能比等了很久的线程先抢到锁。</p>
<p>优点：吞吐量高，新来的线程刚好赶上锁释放就直接用了，不用排队。缺点：可能导致某些线程一直抢不到（线程饥饿）。</p>

<h3>特性3：不可中断</h3>
<p>线程抢不到锁就一直阻塞，不能中途打断，只能一直等下去。</p>
<p>相比之下，ReentrantLock的 <code>lockInterruptibly()</code> 可以响应中断，等烦了可以不等了。</p>

<h2>五、synchronized vs ReentrantLock</h2>

<h3>相同点</h3>
<p>都是可重入的独占锁，都能保证线程安全。</p>

<h3>不同点</h3>

<ul>
<li><b>实现方式</b>：synchronized是JVM内置的（C++实现），ReentrantLock是JDK层面的（Java实现，基于AQS）</li>
<li><b>公平性</b>：synchronized只有非公平，ReentrantLock可以选择公平或非公平</li>
<li><b>可中断</b>：synchronized不可中断，ReentrantLock可以中断</li>
<li><b>功能</b>：ReentrantLock功能更多——尝试获取锁（tryLock）、超时获取、多条件变量</li>
<li><b>释放方式</b>：synchronized自动释放（退出代码块自动释放），ReentrantLock必须手动unlock</li>
</ul>

<h3>怎么选</h3>
<p>一般场景用synchronized就行，简单不会错，JVM一直在优化。只有需要ReentrantLock的特殊功能（公平锁、可中断、超时、多条件）时才用ReentrantLock。</p>

<h2>六、常见坑点</h2>

<h3>坑1：锁错对象</h3>
<p>两个方法用的不是同一把锁，起不到同步效果。比如一个锁this，一个锁静态方法，互不干扰。</p>
<p><b>解法</b>：确保需要互斥的代码用的是同一把锁。</p>

<h3>坑2：锁范围太大</h3>
<p>把不需要同步的代码也包在synchronized里，导致其他线程等待时间过长，性能差。</p>
<p><b>解法</b>：锁的范围越小越好，只锁必要的代码。用同步代码块代替同步方法。</p>

<h3>坑3：字符串常量锁</h3>
<p>用字符串作为锁对象，比如 <code>synchronized("lock")</code>。字符串常量在常量池里是共享的，可能和其他地方的锁冲突。</p>
<p><b>解法</b>：用 <code>private final Object lock = new Object();</code> 作为锁对象。</p>

<h2>知识点总结</h2>

<p>1. synchronized=卫生间门锁，一次进一个，内置锁，自动加解锁。</p>
<p>2. 三种用法：修饰实例方法（锁this）、修饰静态方法（锁Class）、修饰代码块（指定锁对象）。</p>
<p>3. 锁升级：无锁→偏向锁→轻量级锁→重量级锁，逐级升级不可逆。</p>
<p>4. 锁信息存在对象头里，锁的是对象不是代码。</p>
<p>5. 三大特性：可重入、非公平、不可中断。</p>
<p>6. 推荐用同步代码块，锁范围越小越好。</p>
<p>7. 一般场景用synchronized，需要高级功能才用ReentrantLock。</p>

</body>
</html>
