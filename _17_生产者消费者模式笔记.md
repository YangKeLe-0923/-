# 生产者消费者模式笔记

## 一、什么是生产者消费者模式

**定义** **生产者消费者模式=一种经典的多线程并发协作模式，通过共享数据区域解耦生产者和消费者，实现生产与消费的异步处理。**

生产者消费者模式是并发编程中最经典的设计模式之一。简单来说，它包含两类线程：**生产者线程**负责生产数据，**消费者线程**负责消费数据。两者之间通过一个共享的数据区域（类似于仓库）进行通信，生产者生产完数据后直接放入仓库，不需要关心消费者是谁、什么时候消费；消费者只需要从仓库中取数据，也不需要关心生产者是谁、什么时候生产。

### 生活化比喻

想象一下餐厅的后厨运作：厨师就是**生产者**，负责做菜；服务员就是**消费者**，负责给顾客上菜。中间的传菜台就是**共享数据区域**（仓库）。厨师做好菜后把菜放到传菜台上，不用管哪个服务员来端；服务员看到传菜台上有菜就端走，也不用管是哪个厨师做的。

如果传菜台摆满了（仓库满了），厨师就暂时休息（阻塞），等服务员端走一些菜后再继续做；如果传菜台空了（仓库空了），服务员就暂时休息（阻塞），等厨师做好菜放上来再继续端。这就是生产者消费者模式的核心思想。

### 核心角色

生产者消费者模式包含三个核心角色：

1. **生产者（Producer）**：负责生产数据的线程，生产完成后将数据放入共享缓冲区
2. **消费者（Consumer）**：负责消费数据的线程，从共享缓冲区中取出数据进行处理
3. **共享缓冲区（Buffer/仓库）**：生产者和消费者之间的中间桥梁，用于存储生产的数据

共享缓冲区需要具备两个关键的并发协作能力：
- 如果缓冲区已满，阻塞生产者继续生产数据
- 如果缓冲区为空，阻塞消费者继续消费数据

## 二、核心机制

### wait/notify 消息通知机制

Java 中最基础的实现方式是使用 Object 类的 `wait()` 和 `notify()` / `notifyAll()` 方法来实现线程间的通信。

#### 三个核心方法

**1. wait() 方法**

调用 `wait()` 方法会将当前线程置入休眠状态（WAITING 状态），直到其他线程调用 `notify()` 或 `notifyAll()` 进行通知。

**使用要点：**
- 必须在**同步方法或同步块**中调用（线程必须先获得对象的监视器锁）
- 调用后当前线程会**释放锁**
- 如果未持有锁就调用，会抛出 `IllegalMonitorStateException` 异常
- 线程被唤醒后需要重新获取锁才能从 wait 方法返回

**2. notify() 方法**

该方法会从 WAITING 状态的线程中**随机挑选一个**进行通知，使其从等待队列移入同步队列，等待机会再次获取锁。

**使用要点：**
- 同样必须在同步方法或同步块中调用
- 调用后当前线程**不会马上释放锁**，要等退出同步块后才释放
- 只唤醒一个等待线程

**3. notifyAll() 方法**

与 `notify()` 工作方式类似，但差异在于：`notifyAll()` 会唤醒**所有**在该对象上等待的线程，让它们全部从等待队列移入同步队列，竞争获取锁。

#### wait/notify 的三大问题

**问题一：notify 早期通知（通知遗漏）**

**定义** **早期通知=生产者还没开始wait，消费者已经notify了，导致通知没有任何响应，生产者后续wait后永远阻塞。**

比喻：就像你还没到餐厅（还没开始等），服务员就喊"你的菜好了"（发通知），等你到了餐厅开始等菜的时候，就永远等不到了，因为通知已经发过了。

**解决方案：** 添加一个状态标志位，在调用 wait 前先判断状态，如果通知已经发出就不再 wait。

**问题二：wait 条件发生变化**

**定义** **条件变化=线程被唤醒后，wait的条件已经发生了变化，但线程没有再次判断就继续执行，导致程序出错。**

比喻：两个顾客都在等餐（都在 wait），厨师做好了一份菜（notifyAll 唤醒所有人），第一个顾客先拿到菜吃完走了（消费了数据），第二个顾客醒来后直接去拿菜，结果菜已经没了（条件变了），就会出问题。

**解决方案：** 永远在 `while` 循环中调用 wait，而不是用 `if` 判断。这样线程被唤醒后会再次检查条件，如果条件不满足就继续等待。

**问题三："假死"状态**

**定义** **假死=多生产者多消费者场景下，使用notify可能唤醒同类线程，导致所有线程都处于等待状态，程序无法继续运行。**

比喻：餐厅里有多个厨师和多个服务员，传菜台满了，所有厨师都在等；这时一个厨师被唤醒，但他唤醒的是另一个厨师而不是服务员，结果所有厨师还是都在等，没人通知服务员来端菜，整个餐厅就"假死"了。

**解决方案：** 使用 `notifyAll()` 代替 `notify()`，确保能唤醒不同类型的线程。

#### wait/notify 使用范式

```java
// 标准的 wait 调用范式
synchronized (sharedObject) {
    while (condition) {
        sharedObject.wait(); // 释放锁，被唤醒后重新获取锁
    }
    // 条件满足后执行具体操作
}
```

**关键原则：**
1. 永远在 while 循环中判断条件，而不是 if
2. 永远使用 notifyAll 而不是 notify

### BlockingQueue 实现机制

**定义** **BlockingQueue=Java并发包提供的阻塞队列，内置了生产者消费者模式的实现，当队列满时阻塞生产者，队列空时阻塞消费者。**

`BlockingQueue` 是 `java.util.concurrent` 包下的接口，它内部已经实现了线程安全的等待通知机制，开发者可以直接使用，无需手动编写 wait/notify 逻辑。

比喻：BlockingQueue 就像一个自动化的传菜台，菜放满了自动让厨师停手，菜取空了自动让服务员等待，完全不用人工协调。

#### BlockingQueue 的核心方法

| 操作类型 | 抛出异常 | 返回特殊值 | 阻塞等待 | 超时等待 |
|---------|---------|-----------|---------|---------|
| 插入 | add(e) | offer(e) | put(e) | offer(e, time, unit) |
| 移除 | remove() | poll() | take() | poll(time, unit) |
| 检查 | element() | peek() | - | - |

其中 `put()` 和 `take()` 是生产者消费者模式最常用的两个阻塞方法。

## 三、使用步骤

### 方式一：wait/notifyAll 实现

**步骤1：定义共享缓冲区**

使用一个集合（如 LinkedList）作为共享数据缓冲区，设置最大容量。

**步骤2：实现生产者线程**

生产者线程在同步块中循环判断缓冲区是否已满，如果满了就调用 wait() 等待；否则生产数据放入缓冲区，然后调用 notifyAll() 唤醒消费者。

**步骤3：实现消费者线程**

消费者线程在同步块中循环判断缓冲区是否为空，如果空了就调用 wait() 等待；否则从缓冲区取出数据消费，然后调用 notifyAll() 唤醒生产者。

**完整代码示例：**

```java
import java.util.LinkedList;
import java.util.List;
import java.util.Random;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class ProducerConsumer {

    public static void main(String[] args) {
        LinkedList<Integer> linkedList = new LinkedList<>();
        ExecutorService service = Executors.newFixedThreadPool(15);
        
        // 启动5个生产者线程
        for (int i = 0; i < 5; i++) {
            service.submit(new Producer(linkedList, 8));
        }
        
        // 启动10个消费者线程
        for (int i = 0; i < 10; i++) {
            service.submit(new Consumer(linkedList));
        }
    }

    // 生产者
    static class Producer implements Runnable {
        private List<Integer> list;
        private int maxLength;

        public Producer(List<Integer> list, int maxLength) {
            this.list = list;
            this.maxLength = maxLength;
        }

        @Override
        public void run() {
            while (true) {
                synchronized (list) {
                    try {
                        // while循环判断，防止条件变化
                        while (list.size() == maxLength) {
                            System.out.println("生产者" + Thread.currentThread().getName() 
                                + " list已达到最大容量，进行wait");
                            list.wait();
                            System.out.println("生产者" + Thread.currentThread().getName() 
                                + " 退出wait");
                        }
                        // 生产数据
                        Random random = new Random();
                        int i = random.nextInt(100);
                        System.out.println("生产者" + Thread.currentThread().getName() 
                            + " 生产数据：" + i);
                        list.add(i);
                        // 唤醒所有等待线程
                        list.notifyAll();
                    } catch (InterruptedException e) {
                        e.printStackTrace();
                    }
                }
            }
        }
    }

    // 消费者
    static class Consumer implements Runnable {
        private List<Integer> list;

        public Consumer(List<Integer> list) {
            this.list = list;
        }

        @Override
        public void run() {
            while (true) {
                synchronized (list) {
                    try {
                        // while循环判断，防止条件变化
                        while (list.isEmpty()) {
                            System.out.println("消费者" + Thread.currentThread().getName() 
                                + " list为空，进行wait");
                            list.wait();
                            System.out.println("消费者" + Thread.currentThread().getName() 
                                + " 退出wait");
                        }
                        // 消费数据
                        Integer element = list.remove(0);
                        System.out.println("消费者" + Thread.currentThread().getName() 
                            + " 消费数据：" + element);
                        // 唤醒所有等待线程
                        list.notifyAll();
                    } catch (InterruptedException e) {
                        e.printStackTrace();
                    }
                }
            }
        }
    }
}
```

### 方式二：BlockingQueue 实现

**步骤1：选择 BlockingQueue 实现类**

常用实现类：
- `ArrayBlockingQueue`：基于数组的有界阻塞队列
- `LinkedBlockingQueue`：基于链表的阻塞队列（可设置容量，默认 Integer.MAX_VALUE）
- `SynchronousQueue`：不存储元素的阻塞队列，每个插入操作必须等待另一个线程的移除操作

**步骤2：生产者调用 put() 方法放入数据**

队列满时自动阻塞。

**步骤3：消费者调用 take() 方法取出数据**

队列空时自动阻塞。

**完整代码示例：**

```java
import java.util.Random;
import java.util.concurrent.BlockingQueue;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.concurrent.LinkedBlockingQueue;

public class BlockingQueueDemo {

    public static void main(String[] args) {
        // 创建容量为8的阻塞队列
        BlockingQueue<Integer> queue = new LinkedBlockingQueue<>(8);
        ExecutorService service = Executors.newFixedThreadPool(15);

        // 启动5个生产者
        for (int i = 0; i < 5; i++) {
            service.submit(new Producer(queue));
        }

        // 启动10个消费者
        for (int i = 0; i < 10; i++) {
            service.submit(new Consumer(queue));
        }
    }

    static class Producer implements Runnable {
        private BlockingQueue<Integer> queue;

        public Producer(BlockingQueue<Integer> queue) {
            this.queue = queue;
        }

        @Override
        public void run() {
            while (true) {
                try {
                    Random random = new Random();
                    int data = random.nextInt(100);
                    // put方法：队列满时自动阻塞
                    queue.put(data);
                    System.out.println("生产者" + Thread.currentThread().getName() 
                        + " 生产数据：" + data + "，队列大小：" + queue.size());
                    Thread.sleep(100);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }
    }

    static class Consumer implements Runnable {
        private BlockingQueue<Integer> queue;

        public Consumer(BlockingQueue<Integer> queue) {
            this.queue = queue;
        }

        @Override
        public void run() {
            while (true) {
                try {
                    // take方法：队列空时自动阻塞
                    Integer data = queue.take();
                    System.out.println("消费者" + Thread.currentThread().getName() 
                        + " 消费数据：" + data + "，队列大小：" + queue.size());
                    Thread.sleep(200);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }
    }
}
```

## 四、为什么需要生产者消费者模式

### 1. 解耦生产者和消费者

生产者和消费者之间通过共享缓冲区通信，两者互不依赖。生产者不需要知道消费者的存在，也不需要知道数据被谁消费、怎么消费；消费者也不需要知道生产者的存在，只需要从缓冲区取数据。

比喻：电商平台的卖家和买家通过平台仓库解耦。卖家把货放到仓库，不用管谁买；买家从仓库下单，不用管是谁卖的。如果没有仓库（缓冲区），卖家必须直接对接买家，耦合度很高。

### 2. 平衡生产与消费的速度差异

生产者生产数据的速度和消费者消费数据的速度往往不一样。生产者可能时快时慢，消费者也可能时快时慢。通过缓冲区可以起到"削峰填谷"的作用，生产者高峰期生产的数据可以先存到缓冲区，消费者慢慢消费；生产者低谷期时，缓冲区中积累的数据还能保证消费者有活干。

比喻：水库的作用——雨季（生产高峰期）存水，旱季（生产低谷期）放水，保证下游（消费者）水量稳定。

### 3. 提高系统并发处理能力

生产者和消费者是独立的线程，可以并行执行。生产者生产数据的同时，消费者也在消费数据，两者互不阻塞，大大提高了系统的整体吞吐量。

比喻：流水线作业——上一个工位（生产者）加工完的零件放到传送带上（缓冲区），下一个工位（消费者）直接从传送带上取，两个工位同时工作，效率比"做完一个再做下一个"高得多。

### 4. 提高系统稳定性

如果没有缓冲区，生产者生产速度过快会把消费者"压垮"，消费者处理不过来可能导致系统崩溃。有了缓冲区作为缓冲，即使生产速度暂时超过消费速度，也不会导致系统过载，系统可以稳定运行。

## 五、关键技巧

### 技巧一：合理设置缓冲区大小

缓冲区大小是一个关键参数，需要根据实际场景合理设置：
- **缓冲区太小**：缓冲作用有限，生产者容易频繁阻塞
- **缓冲区太大**：占用内存过多，可能造成资源浪费

比喻：餐厅传菜台太小，厨师做两道菜就放不下了，得频繁等；传菜台太大，占地方不说，菜放久了还容易凉。

### 技巧二：使用 while 循环判断条件（重要）

永远不要用 `if` 判断 wait 条件，必须用 `while` 循环。这是因为：
1. 防止条件变化导致的错误
2. 防止虚假唤醒（某些情况下线程可能在没有被通知的情况下自动唤醒）

**错误写法：**
```java
if (list.isEmpty()) {
    list.wait(); // 被唤醒后直接往下执行，可能条件已经变了
}
```

**正确写法：**
```java
while (list.isEmpty()) {
    list.wait(); // 被唤醒后再次检查条件
}
```

### 技巧三：优先使用 notifyAll 而非 notify

`notify()` 只随机唤醒一个线程，可能出现"假死"现象；`notifyAll()` 唤醒所有等待线程，虽然会有一定的性能开销（多个线程竞争锁），但能保证程序的正确性。

**最佳实践：** 除非你非常确定自己在做什么，否则优先使用 `notifyAll()`。

### 技巧四：优先使用 BlockingQueue 而非手动实现

BlockingQueue 是 JDK 官方提供的实现，经过了充分的测试，代码质量高、性能好、边界条件处理完善。在实际开发中，应该优先使用 BlockingQueue 来实现生产者消费者模式，而不是自己手写 wait/notify。

**对比：**

| 对比项 | wait/notify 手动实现 | BlockingQueue 实现 |
|-------|---------------------|-------------------|
| 代码复杂度 | 高，需要处理各种边界情况 | 低，直接调用API即可 |
| 可靠性 | 容易出错（假死、条件变化等） | 高，JDK官方实现 |
| 灵活性 | 高，可以自定义各种逻辑 | 中，遵循队列接口规范 |
| 推荐场景 | 学习原理、特殊定制需求 | 实际项目开发 |

### 技巧五：使用标志位优雅停止线程

生产消费通常是无限循环的，当需要停止线程时，不能直接用 `stop()` 方法（已废弃），应该使用一个 volatile 标志位来控制。

```java
volatile boolean isRunning = true;

public void run() {
    while (isRunning) {
        // 生产或消费逻辑
    }
}

// 停止时设置标志位
public void stop() {
    isRunning = false;
}
```

### 技巧六：生产者消费者数量配比

生产者和消费者的数量需要根据任务特点合理设置：
- 如果生产速度 > 消费速度：增加消费者数量，或减少生产者数量
- 如果消费速度 > 生产速度：增加生产者数量，或减少消费者数量
- 可以通过监控缓冲区的平均大小来动态调整

## 知识点总结

1. **生产者消费者模式**是并发编程的经典模式，通过共享缓冲区解耦生产者和消费者，实现异步处理和流量削峰。

2. **三大核心角色**：生产者（生产数据）、消费者（消费数据）、共享缓冲区（中间桥梁）。

3. **两种主要实现方式**：
   - **wait/notify 方式**：基于 Object 类的等待通知机制，需要手动处理同步和条件判断，适合学习原理
   - **BlockingQueue 方式**：JDK 并发包提供的阻塞队列，内置线程安全实现，适合实际开发

4. **wait/notify 三大坑**：
   - 早期通知 → 用状态标志位解决
   - 条件变化 → 用 while 循环解决
   - 假死状态 → 用 notifyAll 解决

5. **黄金范式**：synchronized + while 循环 + wait/notifyAll，这是被广泛验证的安全写法。

6. **核心价值**：解耦、平衡速度差异、提高并发能力、增强系统稳定性。

7. **实战建议**：实际项目中优先使用 BlockingQueue（如 LinkedBlockingQueue、ArrayBlockingQueue），不要重复造轮子。
