# 并发集合核心知识速览

## 一、CopyOnWriteArrayList

**定义** **CopyOnWriteArrayList** 是J.U.C中的并发List，基于**写时复制（Copy-On-Write）**思想实现，读操作完全无锁，写操作时复制一份新数组来修改。

它本质上就是一块**"班级黑板报"**——大家随时可以看（读无锁），但要改的话，老师会先抄一份新的，在新的上面改，改完了把旧的换掉。改的过程中大家还是看旧的，改完了才能看到新的。

打个比方：COW就像图书馆的公示栏

- 所有人都能随便看（读无锁，不用排队），想看多久看多久
- 要改内容的时候，管理员拿一块新板子在旁边写新内容
- 新内容写完了，直接把旧板子摘下来，挂上新板子（原子替换）
- 正在看旧板子的人继续看旧的，下次来看就是新的了

### 核心原理

- **读操作**：直接读数组，不用加锁，性能极高
- **写操作**：加锁（ReentrantLock），复制整个数组，在新数组上修改，然后把引用指向新数组
- **数组是volatile的**：引用替换后，其他线程能立刻看到新数组

### 代码示例：读多写少场景

```java
public class COWDemo {
    public static void main(String[] args) {
        CopyOnWriteArrayList<String> list = new CopyOnWriteArrayList<>();

        // 写操作：加锁+复制数组
        list.add("Java");
        list.add("Python");
        list.add("Go");

        // 读操作：无锁，直接读
        System.out.println("第0个元素：" + list.get(0));
        System.out.println("大小：" + list.size());

        // 迭代也是读旧快照，不会抛并发修改异常
        for (String s : list) {
            System.out.println(s);
            // 迭代中修改也没事，迭代的是旧快照
            // list.add("C++");  // 不会抛ConcurrentModificationException
        }
    }
}
```

### 优缺点与适用场景

**优点**：读操作完全无锁，读性能极高，迭代安全不会抛异常。

**缺点**：

- **内存占用大**：每次写都要复制整个数组，数据量大的时候内存翻倍
- **最终一致性**：读和写之间有时间差，写了之后其他线程不是立刻能看到
- **写性能差**：写多的场景频繁复制数组，性能很差

**适用场景**：读多写少的场景，比如配置列表、白名单、黑名单等——读很频繁，改的次数很少。

**不适用**：写多读多的场景，或者数据量很大的List。

## 二、并发队列总览

**定义** 并发队列是J.U.C中提供的线程安全的队列实现，分为**阻塞队列**和**非阻塞队列**两大类。

打个比方：并发队列就像快递驿站

- 非阻塞队列=快递柜：满了你就别放了（返回false），空了你就别取了（返回null），不等人
- 阻塞队列=人工驿站：满了快递员等着（阻塞），空了取件人等着（阻塞），有位置/有快递了再通知

### 非阻塞队列

- `ConcurrentLinkedQueue` —— 非阻塞无界队列，基于CAS实现，高并发性能好
- `ConcurrentLinkedDeque` —— 非阻塞双端队列，头尾都能进出

特点：操作不会阻塞，失败了就返回false/null，用CAS保证线程安全。

### 阻塞队列

- `ArrayBlockingQueue` —— 数组实现的有界阻塞队列，一把锁，读写不分离
- `LinkedBlockingQueue` —— 链表实现的阻塞队列，默认无界（Integer.MAX），两把锁，读写分离
- `PriorityBlockingQueue` —— 优先级阻塞队列，按优先级出队，不是先进先出
- `DelayQueue` —— 延迟队列，元素到了过期时间才能出队
- `SynchronousQueue` —— 同步队列，不存储元素，直接交接，来一个送一个
- `LinkedTransferQueue` —— 链表实现的TransferQueue，功能更强

## 三、ConcurrentLinkedQueue

### 是什么

基于链表的无界非阻塞队列，用CAS实现线程安全，高并发下性能比阻塞队列好。

核心方法：

- `offer(e)` —— 入队，成功返回true（无界，永远成功）
- `poll()` —— 出队，空了返回null
- `peek()` —— 看队头，不出队
- `size()` —— 不是原子的，遍历计算，结果可能不准

### 适用场景

高并发下不需要阻塞的场景，比如任务分发、事件收集等。性能好，但没有阻塞功能。

## 四、阻塞队列四组操作

阻塞队列BlockingQueue比普通队列多了"阻塞等待"的能力，一共有四组操作方法：

- **抛异常**：add()满了抛IllegalStateException，remove()空了抛NoSuchElementException
- **返回特殊值**：offer()满了返回false，poll()空了返回null
- **阻塞等待**：put()满了一直等，take()空了一直等
- **超时等待**：offer(e, time, unit)等一段时间，poll(time, unit)等一段时间，超时了返回false/null

### 代码示例：生产者消费者

```java
public class BlockingQueueDemo {
    public static void main(String[] args) {
        BlockingQueue<Integer> queue = new ArrayBlockingQueue<>(5); // 容量5

        // 生产者线程
        new Thread(() -> {
            for (int i = 1; i <= 10; i++) {
                try {
                    queue.put(i);  // 满了就阻塞等
                    System.out.println("生产了：" + i);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }).start();

        // 消费者线程
        new Thread(() -> {
            for (int i = 1; i <= 10; i++) {
                try {
                    Integer val = queue.take();  // 空了就阻塞等
                    System.out.println("消费了：" + val);
                    Thread.sleep(500);  // 消费慢一点，看生产者阻塞
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }).start();
    }
}
```

## 五、常见队列选型

### 有界 vs 无界

**有界队列**（ArrayBlockingQueue）：容量固定，满了就阻塞/返回false。**更安全**，不会因为生产太快导致OOM。推荐生产环境用有界队列。

**无界队列**（LinkedBlockingQueue默认、ConcurrentLinkedQueue）：容量没有上限，生产太快会把内存撑爆。

### 数组 vs 链表

**数组实现**（ArrayBlockingQueue）：连续内存，缓存友好，性能好，但需要预分配空间。一把锁，读写不分离。

**链表实现**（LinkedBlockingQueue）：节点分散，缓存不友好。两把锁（takeLock+putLock），读写分离，并发度更高。

### 选型参考

- 需要阻塞 + 固定容量 → ArrayBlockingQueue
- 需要阻塞 + 读写分离性能 → LinkedBlockingQueue（注意设置容量，别用默认无界）
- 不需要阻塞 + 高性能 → ConcurrentLinkedQueue
- 需要按优先级出队 → PriorityBlockingQueue
- 需要延迟执行 → DelayQueue
- 直接交接，不存储 → SynchronousQueue（线程池CachedThreadPool用的就是它）

## 六、为什么需要并发集合

### 问题1：ArrayList/HashMap线程不安全

普通集合多线程下读写会出现各种问题：数据丢失、死循环、ArrayIndexOutOfBoundsException等等。

并发集合的解决方案：用CAS、锁、写时复制等技术保证线程安全。

### 问题2：Collections.synchronizedXxx性能差

Collections.synchronizedList/Map是包装一层synchronized，相当于整把锁，所有操作排队，性能差。

并发集合的解决方案：降低锁粒度（分段锁、桶级锁）、读写分离、无锁CAS，大幅提升并发性能。

### 问题3：缺少高级并发数据结构

普通集合只有基本的增删改查，没有阻塞队列、优先级队列、延迟队列等高级功能。

并发集合的解决方案：提供了丰富的并发数据结构，满足各种并发编程场景。

## 知识点总结

1. CopyOnWriteArrayList=写时复制，读无锁，写加锁复制数组，读多写少场景用。

2. COW的缺点：内存占用大、最终一致性、写性能差。

3. 并发队列分两类：非阻塞（CAS）和阻塞（锁+条件变量）。

4. 阻塞队列四组操作：抛异常、返回特殊值、阻塞、超时。

5. ArrayBlockingQueue=数组+有界+一把锁；LinkedBlockingQueue=链表+两把锁+读写分离。

6. 生产环境推荐用有界队列，防止OOM。

7. ConcurrentLinkedQueue是非阻塞无界队列，CAS实现，性能好但没有阻塞功能。

8. 选队列口诀：要阻塞用BlockingQueue，要高性能无阻塞用ConcurrentLinkedQueue。
