# 反向代理核心知识速览

## 一、什么是反向代理

**定义** **反向代理 = Reverse Proxy**，是位于客户端和真实服务器之间的中间服务器。

它本质上就是一个**"前台秘书"**——客户端所有请求都先打到反向代理服务器，由它转发给后端真实服务器，再把结果返回给客户端。

打个比方：反向代理就像公司的前台接待

- 客户打电话找某公司，接电话的永远是前台（反向代理），客户不知道具体谁在接
- 前台根据来电内容，把电话转接给对应的部门（后端服务器）
- 部门把结果告诉前台，前台再转告客户——客户始终只和前台打交道

**正向代理 vs 反向代理**：一个代理客户端（翻墙VPN），一个代理服务端（Nginx）。方向相反，本质都是"中间商"。

## 二、核心功能

### 功能1：请求转发

反向代理最基础的能力——接收客户端请求，根据URL路径、域名等规则，转发到不同的后端服务器。

比如访问 `/api/user` 转发到用户服务，访问 `/api/order` 转发到订单服务。

### 功能2：负载均衡

当后端有多台服务器提供相同服务时，反向代理按照一定策略（轮询、加权、IP哈希等）把请求分发到不同服务器，**分摊压力，提高可用性**。

打个比方：就像餐厅的叫号员，客人来了按顺序分配到不同的窗口，不会让某一个窗口排太长的队。

### 功能3：动静分离

静态资源（HTML、CSS、JS、图片）直接由反向代理返回，不需要打到后端服务，**大幅减轻后端服务器压力**。

动态请求（API接口）才转发给后端应用服务器处理。

## 三、Nginx 配置实战

### 第 1 步：最简单的反向代理

把所有请求都转发到本机 8080 端口的服务上：

```nginx
server {
    listen 80;                    # 监听80端口
    server_name example.com;      # 匹配的域名

    location / {
        proxy_pass http://127.0.0.1:8080;  # 转发到后端
        proxy_set_header Host $host;        # 传递原始域名
        proxy_set_header X-Real-IP $remote_addr;  # 传递真实IP
    }
}
```

**注意**：`proxy_set_header` 必须设置，否则后端拿不到真实的客户端IP和域名。

### 第 2 步：按路径转发到不同服务

不同URL前缀转发到不同后端服务，实现简单的网关功能：

```nginx
server {
    listen 80;
    server_name api.example.com;

    location /user/ {
        proxy_pass http://127.0.0.1:8081/;  # 用户服务
    }

    location /order/ {
        proxy_pass http://127.0.0.1:8082/;  # 订单服务
    }

    location /goods/ {
        proxy_pass http://127.0.0.1:8083/;  # 商品服务
    }
}
```

**坑点**：`proxy_pass` 末尾加不加 `/` 区别很大——加了会把 location 前缀截掉再转发，不加会原样转发。写错会导致404。

### 第 3 步：配置负载均衡

定义上游服务器组，用轮询策略分发请求：

```nginx
upstream user_service {
    server 192.168.1.10:8081 weight=2;   # 权重2，分更多流量
    server 192.168.1.11:8081 weight=1;   # 权重1
    server 192.168.1.12:8081 backup;     # 备份服务器，前面都挂了才用
}

server {
    location /user/ {
        proxy_pass http://user_service/;  # 指向upstream名称
    }
}
```

常用负载均衡策略：轮询（默认）、weight加权轮询、ip_hash（按IP哈希，保证同一用户访问同一台）、least_conn（最少连接）。

## 四、为什么需要反向代理

### 问题 1：直接暴露服务不安全

如果把后端服务直接暴露在公网，服务器IP、端口都公开了，黑客可以直接攻击你的应用服务器，风险极高。

有了反向代理：后端服务器全部放在内网，外网只能打到反向代理，相当于多了一层**安全屏障**。反向代理还可以做限流、WAF防火墙、SSL加密等安全措施。

### 问题 2：单台服务器扛不住

用户量上来后，单台应用服务器性能有限，扛不住高并发。如果直接让用户连接不同的服务器，用户体验极差——还要用户自己选服务器？

有了反向代理：用户只需要记住一个域名，反向代理自动把请求分到多台后端服务器，**横向扩展能力**大大增强。加机器只需要在upstream里加一行配置。

### 问题 3：前后端分离跨域

前后端分离项目中，前端页面和后端API不在同一个域名下，浏览器会因为同源策略产生跨域问题。

有了反向代理：前端和后端都通过同一个域名访问，反向代理根据路径区分静态资源和API，**天然解决跨域**，不需要后端额外配置CORS。

### 问题 4：静态资源占用后端性能

如果所有请求都打到后端Java服务，包括图片、CSS、JS这些静态文件，会浪费大量后端线程和内存，拖慢动态接口响应。

有了反向代理：静态资源直接由Nginx返回（Nginx处理静态资源性能极强），动态请求才转发给后端，**资源利用率大幅提升**。

## 五、关键技巧

### 技巧1：解决跨域的标准配置

在Nginx层统一处理跨域，不用每个后端服务都配：

```nginx
location /api/ {
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
    add_header Access-Control-Allow-Headers 'Content-Type, Authorization';

    if ($request_method = OPTIONS) {
        return 204;   # 预检请求直接返回，不转发
    }

    proxy_pass http://backend;
}
```

### 技巧2：gzip压缩减少流量

开启gzip压缩，文本类资源可以压缩到原来的 1/3 ~ 1/5，显著减少带宽消耗：

```
gzip on;
gzip_types text/plain text/css application/json application/javascript;
gzip_min_length 1k;    # 小于1k不压缩（压缩反而更大）
gzip_comp_level 5;     # 压缩级别1-9，5是性价比最高
```

## 知识点总结

1. 反向代理=前台秘书，客户端只和代理打交道，不知道真实服务器是谁。

2. 三大核心功能：请求转发、负载均衡、动静分离。

3. Nginx配置三要素：server块定义服务、location匹配路径、proxy_pass转发目标。

4. proxy_pass末尾加不加/是个坑，加了会截掉location前缀。

5. 负载均衡策略：轮询、加权轮询、ip_hash、least_conn。

6. 四大价值：安全屏障、横向扩展、解决跨域、性能提升。

7. 常用技巧：Nginx层统一处理跨域、开启gzip压缩。
