# 线程池核心知识速览

## 一、什么是线程池

**定义** **线程池 = Thread Pool**，是一种基于池化思想管理线程的技术，预先创建一批线程，任务来了直接分配线程执行，执行完线程不销毁，留着等下一个任务。

它本质上就是一个**"公司人力资源池"**——公司不招一个活开一个人，而是有一批固定员工（核心线程），活多了再招几个临时工（非核心线程），活少了临时工过段时间就辞退，正式工一直留着。

打个比方：线程池就像公司员工

- 核心线程 = 正式员工，一直留在公司（除非设置了allowCoreThreadTimeOut）
- 非核心线程 = 临时工，活多了招来帮忙，闲了一段时间就辞退
- 任务队列 = 待办任务清单，员工先做清单里的任务
- 拒绝策略 = 活太多实在干不完了，怎么办（拒接、报错、调用者自己干等）

## 二、七大核心参数

ThreadPoolExecutor的构造方法有7个参数，每个都很重要：

### 1. corePoolSize（核心线程数）

线程池中一直保持的线程数量，即使闲着也不会销毁（默认）。相当于正式员工编制。

### 2. maximumPoolSize（最大线程数）

线程池最多能创建多少个线程。核心线程+非核心线程的总数上限。相当于公司最大员工数。

### 3. keepAliveTime（空闲存活时间）

非核心线程空闲多久就被回收（辞退临时工）。默认只作用于非核心线程。

### 4. unit（时间单位）

keepAliveTime的时间单位，比如TimeUnit.SECONDS。

### 5. workQueue（任务队列）

存放待执行任务的阻塞队列。核心线程都在忙的时候，新任务先放队列里等。

### 6. threadFactory（线程工厂）

创建线程的工厂，可以给线程起名字、设置优先级、是否守护线程等。

### 7. handler（拒绝策略）

队列满了+线程数达到最大时，新任务怎么办的策略。

## 三、执行流程

新任务提交到线程池后，按以下顺序判断：

当前线程数
当前线程数 >= corePoolSize → 尝试把任务放进任务队列
队列放满了 → 创建非核心线程执行任务
线程数达到 maximumPoolSize → 执行拒绝策略

注意顺序：**先核心线程 → 再队列 → 再非核心线程 → 最后拒绝**。不是先开满所有线程再进队列。

### 代码示例：自定义线程池

```java
// 推荐：手动创建线程池，不要用Executors
ThreadPoolExecutor pool = new ThreadPoolExecutor(
    5,                          // corePoolSize 核心线程数
    10,                         // maximumPoolSize 最大线程数
    60,                         // keepAliveTime 空闲存活时间
    TimeUnit.SECONDS,           // unit 时间单位
    new ArrayBlockingQueue<>(100),  // workQueue 有界队列，防止OOM
    new ThreadFactory() {       // threadFactory 线程工厂，给线程起名
        private int num = 0;
        @Override
        public Thread newThread(Runnable r) {
            return new Thread(r, "我的线程-" + num++);
        }
    },
    new ThreadPoolExecutor.CallerRunsPolicy()  // handler 拒绝策略
);

// 提交任务
pool.execute(() -> {
    System.out.println("任务执行中：" + Thread.currentThread().getName());
});

// 关闭线程池
pool.shutdown();
```

## 四、四种拒绝策略

- **AbortPolicy**（默认）：直接抛 RejectedExecutionException 异常，告诉调用者"我扛不住了"
- **CallerRunsPolicy**：调用者自己跑（提交任务的线程自己执行这个任务），相当于一种回压机制
- **DiscardPolicy**：直接扔掉，不抛异常也不执行，任务就丢了（慎用）
- **DiscardOldestPolicy**：扔掉队列里最老的任务，把新任务加进去（也慎用）

生产环境推荐自定义拒绝策略，比如记录日志+持久化任务，保证任务不丢。

## 五、线程复用原理

### 为什么线程能复用

线程池里的Worker线程不是执行完一个任务就结束，而是一直在一个while循环里，不断从任务队列里取下一个任务执行。

核心逻辑：

```
// Worker线程的run方法大致逻辑
while (task != null || (task = getTask()) != null) {
    task.run();  // 执行任务
    task = null;
}
// 退出循环 → 线程结束
```

### getTask() 的关键

- 核心线程：用 `workQueue.take()` 阻塞等待，没任务就一直等，所以核心线程不会死
- 非核心线程：用 `workQueue.poll(keepAliveTime)` 超时等待，等了keepAliveTime还没任务就返回null，循环退出，线程结束

这就是为什么核心线程一直活着，非核心线程超时就回收——一个用take一直等，一个用poll超时等。

## 六、Executors的坑

### 为什么不推荐用Executors

Executors提供了几个便捷方法创建线程池，但都有坑，《阿里巴巴Java开发手册》明确禁止使用。

- **newFixedThreadPool**：LinkedBlockingQueue默认无界，任务堆积可能OOM
- **newCachedThreadPool**：最大线程数是Integer.MAX，来一个开一个，可能创建大量线程OOM
- **newSingleThreadExecutor**：单线程+无界队列，任务堆积可能OOM
- **newScheduledThreadPool**：最大线程数Integer.MAX，可能OOM

**推荐**：用ThreadPoolExecutor手动创建，指定有界队列和合理的线程数，出了问题也知道是哪的问题。

## 七、线程数怎么设

### CPU密集型任务

任务主要是计算，CPU跑满。线程数设置为 CPU核心数 + 1。

+1是为了防止某个线程偶然阻塞（缺页中断等），多一个线程能保证CPU利用率。

### IO密集型任务

任务大部分时间在等IO（数据库、网络、文件），CPU闲着。线程数可以设置大一些。

经验公式：线程数 = CPU核心数 × (1 + 等待时间/计算时间)

一般Web应用都是IO密集型，线程数设得大一些，比如20~50，具体压测调整。

### 注意事项

没有万能公式，最佳实践是**压测**。根据实际业务场景，设置不同的参数，看QPS和响应时间，找到最优配置。

## 知识点总结

1. 线程池=公司人力池，核心线程=正式工，非核心线程=临时工。

2. 七大参数：coreSize、maxSize、keepAliveTime、unit、workQueue、threadFactory、handler。

3. 执行流程：核心线程→队列→非核心线程→拒绝，顺序不能搞反。

4. 四种拒绝策略：抛异常、调用者跑、直接丢、丢最老的。

5. 线程复用：Worker线程while循环不断从队列取任务执行，核心线程take阻塞，非核心线程poll超时。

6. 不要用Executors创建线程池，用ThreadPoolExecutor手动创建，用有界队列。

7. CPU密集型线程数≈CPU核心数+1，IO密集型可以大一些，最终靠压测调优。

8. 用完线程池要shutdown，不然程序不会退出。
