# 定时任务线程池笔记

## 一、什么是ScheduledThreadPoolExecutor

**定义** **ScheduledThreadPoolExecutor** 是JUC包下的定时任务线程池，继承自ThreadPoolExecutor，支持延迟执行和周期性执行任务。

可以理解为"会定时交作业的线程池"——不仅能提交任务立即执行，还能指定几点几分执行，或者每隔多久重复执行。

打个比方：就像**带闹钟功能的工人调度系统**

- 普通线程池是"来了活就派工人干"
- 定时线程池是"可以设置闹钟，到点了自动派工人干活"，而且还能设置"每隔一小时干一次"
- JDK1.5之前用Timer类，就像一个老钟表，单线程、容易坏、时间不准
- ScheduledThreadPoolExecutor就像智能闹钟，多线程、抗造、精确

Timer的四大缺陷（为什么被替代）：单线程执行（一个任务卡住影响所有）、基于绝对时间（改系统时间会乱）、不捕获异常（一个任务崩了全停）、功能单一（只有固定间隔）。

## 二、核心方法对比

### 三种调度方式

```
ScheduledExecutorService pool =
    new ScheduledThreadPoolExecutor(2);  // 核心线程数2

// 1. 延迟3秒后执行一次（只执行一次）
pool.schedule(task, 3, TimeUnit.SECONDS);

// 2. 初始延迟1秒后，每隔2秒执行一次（固定速率）
// 从任务开始时间算起，任务执行时间超过period则结束后立即执行下一次
pool.scheduleAtFixedRate(task, 1, 2, TimeUnit.SECONDS);

// 3. 初始延迟1秒后，每次任务完成后等2秒再执行（固定时延）
// 从任务结束时间算起，每次间隔都是delay
pool.scheduleWithFixedDelay(task, 1, 2, TimeUnit.SECONDS);
```

### FixedRate vs FixedDelay

这两个最容易搞混，一句话区分：

- **FixedRate**：按"开始时间"算间隔，比如每隔1小时一次——1点开始，2点开始，3点开始。如果某次任务执行了40分钟，下一次就只剩20分钟间隔。
- **FixedDelay**：按"结束时间"算间隔，比如每次结束后等1小时——1点开始1点半结束，2点半开始3点结束，4点开始……每次间隔都是完整的1小时。

### 使用示例：定时消息发送

```java
public class MsgScheduler {
    private static final ScheduledExecutorService executor =
        new ScheduledThreadPoolExecutor(1, Executors.defaultThreadFactory());
    private static SimpleDateFormat df =
        new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");

    public static void main(String[] args) {
        // 初始延迟1秒，之后每次任务完成后等1秒再执行
        executor.scheduleWithFixedDelay(() -> {
            if (haveMsgToSend()) {
                System.out.println(df.format(new Date()));
                System.out.println("发送定时消息");
            }
        }, 1, 1, TimeUnit.SECONDS);
    }

    private static boolean haveMsgToSend() {
        // 查询数据库有没有需要发送的消息
        return true;
    }
}
```

## 三、实现原理

### 类结构

ScheduledThreadPoolExecutor继承自ThreadPoolExecutor，所以它的核心机制和普通线程池一样：核心线程、任务队列、worker线程。

特殊之处在于：

- 队列用的是 **DelayedWorkQueue**（无界优先队列），不是普通的阻塞队列
- `maximumPoolSize` 设为 `Integer.MAX_VALUE`——但因为队列是无界的，非核心线程永远不会创建，实际线程数最多等于corePoolSize
- `keepAliveTime` 为0

### ScheduledFutureTask：定时任务的封装

提交的任务会被包装成 `ScheduledFutureTask`，它包含三个关键字段：

- `time`：任务下次执行的时间（纳秒）
- `period`：周期值，正数=固定速率，负数=固定时延，0=非周期任务（只执行一次）
- `sequenceNumber`：任务序号，执行时间相同时按序号排序（先提交的排前面）

### 周期任务的执行流程

```
public void run() {
    boolean periodic = isPeriodic();
    if (!canRunInCurrentRunState(periodic))
        cancel(false);               // 线程池状态不对，取消任务
    else if (!periodic)
        super.run();                 // 非周期：执行一次就完了
    else if (super.runAndReset()) {  // 周期：执行但不设结果（保持NEW状态）
        setNextRunTime();            // 计算下次执行时间
        reExecutePeriodic(outerTask); // 重新入队，等待下一次
    }
}
```

注意周期任务用的是 `runAndReset` 而不是 `run`——执行完不设置结果，任务状态保持NEW，这样才能反复执行。

## 四、DelayedWorkQueue与Leader-Follower

### DelayedWorkQueue

DelayedWorkQueue是ScheduledThreadPoolExecutor内部的等待队列，是一个**无界优先队列**，底层用堆实现。

- 队首永远是"最近要执行的任务"——时间最早的排最前面
- 排序规则：先按执行时间time从小到大，时间相同按sequenceNumber从小到大（先提交的先执行）
- 无界队列，所以不会触发非核心线程创建

### Leader-Follower模式

这是DelayedWorkQueue的核心优化机制，翻译过来叫"领导者-追随者"模式。

打个比方：就像**值班制度**

- 有多个工人等着干活，但队首任务还没到点
- 如果所有人都死盯着队首等时间到，那就是浪费——大家都在空等
- 所以选一个"值班的"（leader），只有它等着队首任务的到期时间
- 其他人（follower）就安心睡觉去，不用空耗CPU
- 值班的等到任务了，就叫醒一个睡觉的来当下一个值班的，自己去执行任务
- 这样同一时间最多只有一个线程在等待计时，节省资源

## 五、关键技巧与坑点

### 技巧1：核心线程数设置

因为DelayedWorkQueue是无界队列，最大线程数实际上就等于corePoolSize。根据任务类型设置：

- 任务都是CPU密集型：核心线程数 = CPU核数
- 任务有IO等待：核心线程数 = CPU核数 * 2 或更多
- 任务不多的话，设1也可以

### 技巧2：优雅关闭定时任务

关闭线程池时，周期任务是否继续执行取决于运行状态判断。用 `shutdown()` 后正在执行的任务会继续执行完，已排队的任务要看配置。如果想立刻停，用 `shutdownNow()`。

### 坑点1：任务抛异常会静默终止

**现象**：周期任务跑着跑着就不执行了，也没报错。

**原因**：任务执行时抛出了未捕获的异常，ScheduledFutureTask的run方法不会继续重入队，任务就停了。

**解法**：在Runnable里用try-catch把整个任务包起来，异常自己处理掉，别往外抛。

### 坑点2：FixedRate任务执行超时会"赶时间"

**现象**：用了scheduleAtFixedRate，任务执行时间偶尔超过period，之后任务就挤在一起执行了。

**原因**：FixedRate是从任务开始时间算间隔的，如果任务执行了很久，下一次的开始时间早就到了，任务结束后会立即执行下一次，把"欠的"补上。

**解法**：如果希望每次间隔都是固定的（不管任务执行多久），用scheduleWithFixedDelay。

### 坑点3：maximumPoolSize设了也没用

**现象**：设置了maximumPoolSize为10，但实际线程数永远只有corePoolSize那么多。

**原因**：DelayedWorkQueue是无界队列，任务永远不会把队列塞满，线程池规则是"队列满了才创建非核心线程"，所以非核心线程永远不会创建。

**解法**：理解这个特性就好，不用纠结，调整corePoolSize来控制并发数。

### 坑点4：任务取消了还可能执行

**现象**：调用了cancel取消任务，任务还是执行了一次。

**原因**：如果取消时任务已经到点了，worker线程已经把它取出来了，那还是会执行。cancel只是把它从队列里移除，已经取出来的没办法。

**解法**：取消后如果需要确保不执行，可以加一个boolean标志位，任务执行前检查一下。

## 六、总结

- ScheduledThreadPoolExecutor = 带定时功能的线程池，替代老旧的Timer类
- 三种调度：schedule（一次延迟）、scheduleAtFixedRate（固定速率）、scheduleWithFixedDelay（固定时延）
- FixedRate从开始时间算间隔，FixedDelay从结束时间算间隔，别搞混
- 底层用DelayedWorkQueue无界优先队列，队首是最近要执行的任务
- Leader-Follower模式：只有一个线程等待队首任务到期，其他线程休息，节省资源
- 周期任务用runAndReset执行，不设置结果，保持NEW状态才能反复执行
- 任务里的异常一定要捕获，否则周期任务会静默停止
- 无界队列导致maximumPoolSize无效，实际并发数由corePoolSize决定
