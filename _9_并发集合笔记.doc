<html xmlns:v="urn:schemas-microsoft-com:vml"
xmlns:o="urn:schemas-microsoft-com:office:office"
xmlns:w="urn:schemas-microsoft-com:office:word"
xmlns:m="http://schemas.microsoft.com/office/2004/12/omml"
xmlns="http://www.w3.org/TR/REC-html40">

<head>
<meta http-equiv=Content-Type content="text/html; charset=utf-8">
<meta name=ProgId content=Word.Document>
<style>
body { font-family: "微软雅黑", "Microsoft YaHei", "SimSun", Arial; font-size: 11pt; line-height: 1.8; color: #333333; }
h1 { font-size: 18pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei; text-align: center;
     border-bottom: 2.25pt solid #2C3E50; padding-bottom: 6pt; }
h2 { font-size: 14pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei;
     border-bottom: 1.0pt solid #BDC3C7; padding-bottom: 3pt; margin-top: 20pt; }
h3 { font-size: 12pt; font-weight: bold; font-family: "微软雅黑", "Microsoft YaHei", SimHei; margin-top: 16pt; }
code { font-family: Consolas, "Courier New"; background: #f5f5f5; padding: 1px 3px; font-size: 10pt; }
pre { font-family: Consolas, "Courier New"; background: #f8f8f8; padding: 10pt;
      border: 1px solid #ddd; font-size: 10pt; white-space: pre-wrap; line-height: 1.5; }
ul { margin-top: 6pt; margin-bottom: 6pt; }
li { margin-bottom: 3pt; }
.tag1 { display: inline-block; background: #E8F4F8; padding: 2px 8px; border-radius: 3px;
       font-size: 9pt; color: #2C3E50; margin-right: 6pt; }
</style>
</head>
<body>

<h1>并发集合核心知识速览</h1>

<h2>一、CopyOnWriteArrayList</h2>

<p><span class="tag1">定义</span> <b>CopyOnWriteArrayList</b> 是J.U.C中的并发List，基于<b>写时复制（Copy-On-Write）</b>思想实现，读操作完全无锁，写操作时复制一份新数组来修改。</p>
<p>它本质上就是一块<b>"班级黑板报"</b>——大家随时可以看（读无锁），但要改的话，老师会先抄一份新的，在新的上面改，改完了把旧的换掉。改的过程中大家还是看旧的，改完了才能看到新的。</p>
<p>打个比方：COW就像图书馆的公示栏</p>

<ul>
<li>所有人都能随便看（读无锁，不用排队），想看多久看多久</li>
<li>要改内容的时候，管理员拿一块新板子在旁边写新内容</li>
<li>新内容写完了，直接把旧板子摘下来，挂上新板子（原子替换）</li>
<li>正在看旧板子的人继续看旧的，下次来看就是新的了</li>
</ul>

<h3>核心原理</h3>

<ul>
<li><b>读操作</b>：直接读数组，不用加锁，性能极高</li>
<li><b>写操作</b>：加锁（ReentrantLock），复制整个数组，在新数组上修改，然后把引用指向新数组</li>
<li><b>数组是volatile的</b>：引用替换后，其他线程能立刻看到新数组</li>
</ul>

<h3>代码示例：读多写少场景</h3>

<pre>
public class COWDemo {
    public static void main(String[] args) {
        CopyOnWriteArrayList&lt;String&gt; list = new CopyOnWriteArrayList&lt;&gt;();

        // 写操作：加锁+复制数组
        list.add("Java");
        list.add("Python");
        list.add("Go");

        // 读操作：无锁，直接读
        System.out.println("第0个元素：" + list.get(0));
        System.out.println("大小：" + list.size());

        // 迭代也是读旧快照，不会抛并发修改异常
        for (String s : list) {
            System.out.println(s);
            // 迭代中修改也没事，迭代的是旧快照
            // list.add("C++");  // 不会抛ConcurrentModificationException
        }
    }
}
</pre>

<h3>优缺点与适用场景</h3>

<p><b>优点</b>：读操作完全无锁，读性能极高，迭代安全不会抛异常。</p>
<p><b>缺点</b>：</p>

<ul>
<li><b>内存占用大</b>：每次写都要复制整个数组，数据量大的时候内存翻倍</li>
<li><b>最终一致性</b>：读和写之间有时间差，写了之后其他线程不是立刻能看到</li>
<li><b>写性能差</b>：写多的场景频繁复制数组，性能很差</li>
</ul>

<p><b>适用场景</b>：读多写少的场景，比如配置列表、白名单、黑名单等——读很频繁，改的次数很少。</p>
<p><b>不适用</b>：写多读多的场景，或者数据量很大的List。</p>

<h2>二、并发队列总览</h2>

<p><span class="tag1">定义</span> 并发队列是J.U.C中提供的线程安全的队列实现，分为<b>阻塞队列</b>和<b>非阻塞队列</b>两大类。</p>
<p>打个比方：并发队列就像快递驿站</p>

<ul>
<li>非阻塞队列=快递柜：满了你就别放了（返回false），空了你就别取了（返回null），不等人</li>
<li>阻塞队列=人工驿站：满了快递员等着（阻塞），空了取件人等着（阻塞），有位置/有快递了再通知</li>
</ul>

<h3>非阻塞队列</h3>

<ul>
<li><code>ConcurrentLinkedQueue</code> —— 非阻塞无界队列，基于CAS实现，高并发性能好</li>
<li><code>ConcurrentLinkedDeque</code> —— 非阻塞双端队列，头尾都能进出</li>
</ul>

<p>特点：操作不会阻塞，失败了就返回false/null，用CAS保证线程安全。</p>

<h3>阻塞队列</h3>

<ul>
<li><code>ArrayBlockingQueue</code> —— 数组实现的有界阻塞队列，一把锁，读写不分离</li>
<li><code>LinkedBlockingQueue</code> —— 链表实现的阻塞队列，默认无界（Integer.MAX），两把锁，读写分离</li>
<li><code>PriorityBlockingQueue</code> —— 优先级阻塞队列，按优先级出队，不是先进先出</li>
<li><code>DelayQueue</code> —— 延迟队列，元素到了过期时间才能出队</li>
<li><code>SynchronousQueue</code> —— 同步队列，不存储元素，直接交接，来一个送一个</li>
<li><code>LinkedTransferQueue</code> —— 链表实现的TransferQueue，功能更强</li>
</ul>

<h2>三、ConcurrentLinkedQueue</h2>

<h3>是什么</h3>
<p>基于链表的无界非阻塞队列，用CAS实现线程安全，高并发下性能比阻塞队列好。</p>
<p>核心方法：</p>

<ul>
<li><code>offer(e)</code> —— 入队，成功返回true（无界，永远成功）</li>
<li><code>poll()</code> —— 出队，空了返回null</li>
<li><code>peek()</code> —— 看队头，不出队</li>
<li><code>size()</code> —— 不是原子的，遍历计算，结果可能不准</li>
</ul>

<h3>适用场景</h3>
<p>高并发下不需要阻塞的场景，比如任务分发、事件收集等。性能好，但没有阻塞功能。</p>

<h2>四、阻塞队列四组操作</h2>

<p>阻塞队列BlockingQueue比普通队列多了"阻塞等待"的能力，一共有四组操作方法：</p>

<ul>
<li><b>抛异常</b>：add()满了抛IllegalStateException，remove()空了抛NoSuchElementException</li>
<li><b>返回特殊值</b>：offer()满了返回false，poll()空了返回null</li>
<li><b>阻塞等待</b>：put()满了一直等，take()空了一直等</li>
<li><b>超时等待</b>：offer(e, time, unit)等一段时间，poll(time, unit)等一段时间，超时了返回false/null</li>
</ul>

<h3>代码示例：生产者消费者</h3>

<pre>
public class BlockingQueueDemo {
    public static void main(String[] args) {
        BlockingQueue&lt;Integer&gt; queue = new ArrayBlockingQueue&lt;&gt;(5); // 容量5

        // 生产者线程
        new Thread(() -&gt; {
            for (int i = 1; i &lt;= 10; i++) {
                try {
                    queue.put(i);  // 满了就阻塞等
                    System.out.println("生产了：" + i);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }).start();

        // 消费者线程
        new Thread(() -&gt; {
            for (int i = 1; i &lt;= 10; i++) {
                try {
                    Integer val = queue.take();  // 空了就阻塞等
                    System.out.println("消费了：" + val);
                    Thread.sleep(500);  // 消费慢一点，看生产者阻塞
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }).start();
    }
}
</pre>

<h2>五、常见队列选型</h2>

<h3>有界 vs 无界</h3>
<p><b>有界队列</b>（ArrayBlockingQueue）：容量固定，满了就阻塞/返回false。<b>更安全</b>，不会因为生产太快导致OOM。推荐生产环境用有界队列。</p>
<p><b>无界队列</b>（LinkedBlockingQueue默认、ConcurrentLinkedQueue）：容量没有上限，生产太快会把内存撑爆。</p>

<h3>数组 vs 链表</h3>
<p><b>数组实现</b>（ArrayBlockingQueue）：连续内存，缓存友好，性能好，但需要预分配空间。一把锁，读写不分离。</p>
<p><b>链表实现</b>（LinkedBlockingQueue）：节点分散，缓存不友好。两把锁（takeLock+putLock），读写分离，并发度更高。</p>

<h3>选型参考</h3>

<ul>
<li>需要阻塞 + 固定容量 → ArrayBlockingQueue</li>
<li>需要阻塞 + 读写分离性能 → LinkedBlockingQueue（注意设置容量，别用默认无界）</li>
<li>不需要阻塞 + 高性能 → ConcurrentLinkedQueue</li>
<li>需要按优先级出队 → PriorityBlockingQueue</li>
<li>需要延迟执行 → DelayQueue</li>
<li>直接交接，不存储 → SynchronousQueue（线程池CachedThreadPool用的就是它）</li>
</ul>

<h2>六、为什么需要并发集合</h2>

<h3>问题1：ArrayList/HashMap线程不安全</h3>
<p>普通集合多线程下读写会出现各种问题：数据丢失、死循环、ArrayIndexOutOfBoundsException等等。</p>
<p>并发集合的解决方案：用CAS、锁、写时复制等技术保证线程安全。</p>

<h3>问题2：Collections.synchronizedXxx性能差</h3>
<p>Collections.synchronizedList/Map是包装一层synchronized，相当于整把锁，所有操作排队，性能差。</p>
<p>并发集合的解决方案：降低锁粒度（分段锁、桶级锁）、读写分离、无锁CAS，大幅提升并发性能。</p>

<h3>问题3：缺少高级并发数据结构</h3>
<p>普通集合只有基本的增删改查，没有阻塞队列、优先级队列、延迟队列等高级功能。</p>
<p>并发集合的解决方案：提供了丰富的并发数据结构，满足各种并发编程场景。</p>

<h2>知识点总结</h2>

<p>1. CopyOnWriteArrayList=写时复制，读无锁，写加锁复制数组，读多写少场景用。</p>
<p>2. COW的缺点：内存占用大、最终一致性、写性能差。</p>
<p>3. 并发队列分两类：非阻塞（CAS）和阻塞（锁+条件变量）。</p>
<p>4. 阻塞队列四组操作：抛异常、返回特殊值、阻塞、超时。</p>
<p>5. ArrayBlockingQueue=数组+有界+一把锁；LinkedBlockingQueue=链表+两把锁+读写分离。</p>
<p>6. 生产环境推荐用有界队列，防止OOM。</p>
<p>7. ConcurrentLinkedQueue是非阻塞无界队列，CAS实现，性能好但没有阻塞功能。</p>
<p>8. 选队列口诀：要阻塞用BlockingQueue，要高性能无阻塞用ConcurrentLinkedQueue。</p>

</body>
</html>
