# synchronized核心知识速览

## 一、什么是synchronized

**定义** **synchronized** 是Java的内置锁关键字，用来保证同一时刻只有一个线程执行被修饰的代码，解决多线程的线程安全问题。

它本质上就是一把**"卫生间门锁"**——一次只能进一个人，进去后锁门，出来后开门，下一个人才能进。

打个比方：synchronized就像公共卫生间

- 共享资源是卫生间（临界区代码），大家都要用
- 第一个人进去，锁上门（加锁），其他人只能在外面等（阻塞）
- 里面的人用完了，开门出来（释放锁），下一个人才能进去
- 谁先抢到不一定，synchronized是非公平锁，谁跑得快谁先进

## 二、三种使用方式

### 方式1：修饰实例方法

锁的是当前对象实例 `this`，同一个对象的synchronized方法互斥。

```java
public class Counter {
    private int count = 0;

    // 锁的是调用这个方法的对象实例
    public synchronized void add() {
        count++;  // 同一时间只有一个线程能执行
    }
}
```

**注意**：两个不同对象的synchronized方法不互斥，因为锁的不是同一把锁。

### 方式2：修饰静态方法

锁的是类的Class对象（`Counter.class`），类的所有实例共用一把锁。

```java
public class Counter {
    private static int count = 0;

    // 锁的是Counter.class对象，全局一把锁
    public static synchronized void add() {
        count++;
    }
}
```

### 方式3：修饰代码块

最灵活的方式，自己指定锁对象，只锁住需要同步的代码，粒度更细。

```
public void doSomething() {
    // 不需要同步的代码...

    synchronized (this) {       // 指定锁对象
        // 需要同步的代码块
        count++;
    }

    // 不需要同步的代码...
}
```

**推荐**：尽量用同步代码块而不是同步方法，锁的范围越小越好，减少阻塞时间。

## 三、锁升级原理

### 为什么要有锁升级

synchronized早期是重量级锁，每次加锁释放锁都要操作系统帮忙，性能很差。

JDK6之后做了优化，引入了**锁升级**机制——根据竞争情况逐级升级，从无锁→偏向锁→轻量级锁→重量级锁，竞争越激烈，锁级别越高。

打个比方：就像会议室管理

- 偏向锁=你有固定工位，默认就是你的，不用每次申请
- 轻量级锁=会议室人少，大家CAS抢一下，抢到就用
- 重量级锁=人太多抢不到，排号等吧，有专人管理

### 锁对象头

锁的信息存在对象头（Mark Word）里，不是存在synchronized关键字本身。对象头里记录了当前锁的状态和持有者。

这也是为什么"锁的是对象不是代码"——锁是对象的属性，代码只是申请使用这把锁。

### 四种锁状态

- **无锁**：没有线程竞争，对象头里存的是对象哈希码等信息
- **偏向锁**：只有一个线程用这把锁，把线程ID记在对象头里，下次这个线程来直接用，不用CAS
- **轻量级锁**：有少量线程竞争，线程用CAS方式尝试获取锁，抢不到就自旋一会儿（忙等）
- **重量级锁**：竞争激烈，自旋多次都抢不到，升级成重量级锁，抢不到的线程挂起（阻塞）

**注意**：锁只能升级不能降级（偏向→轻量→重量，不可逆）。除非进入安全点重置偏向状态。

## 四、核心特性

### 特性1：可重入

同一个线程可以多次获取同一把锁，不会自己把自己锁死。锁对象头里有个计数器，加锁一次+1，释放一次-1，减到0才真正释放。

```
public synchronized void methodA() {
    methodB();  // 同一个线程，已经有锁了，可以直接进
}

public synchronized void methodB() {
    // 可重入：不会死锁
}
```

没有可重入的话，上面代码会死锁——methodA拿到锁后调methodB，methodB又要等锁，结果就是自己等自己。

### 特性2：非公平

synchronized默认是非公平锁——后来的线程可能比等了很久的线程先抢到锁。

优点：吞吐量高，新来的线程刚好赶上锁释放就直接用了，不用排队。缺点：可能导致某些线程一直抢不到（线程饥饿）。

### 特性3：不可中断

线程抢不到锁就一直阻塞，不能中途打断，只能一直等下去。

相比之下，ReentrantLock的 `lockInterruptibly()` 可以响应中断，等烦了可以不等了。

## 五、synchronized vs ReentrantLock

### 相同点

都是可重入的独占锁，都能保证线程安全。

### 不同点

- **实现方式**：synchronized是JVM内置的（C++实现），ReentrantLock是JDK层面的（Java实现，基于AQS）
- **公平性**：synchronized只有非公平，ReentrantLock可以选择公平或非公平
- **可中断**：synchronized不可中断，ReentrantLock可以中断
- **功能**：ReentrantLock功能更多——尝试获取锁（tryLock）、超时获取、多条件变量
- **释放方式**：synchronized自动释放（退出代码块自动释放），ReentrantLock必须手动unlock

### 怎么选

一般场景用synchronized就行，简单不会错，JVM一直在优化。只有需要ReentrantLock的特殊功能（公平锁、可中断、超时、多条件）时才用ReentrantLock。

## 六、常见坑点

### 坑1：锁错对象

两个方法用的不是同一把锁，起不到同步效果。比如一个锁this，一个锁静态方法，互不干扰。

**解法**：确保需要互斥的代码用的是同一把锁。

### 坑2：锁范围太大

把不需要同步的代码也包在synchronized里，导致其他线程等待时间过长，性能差。

**解法**：锁的范围越小越好，只锁必要的代码。用同步代码块代替同步方法。

### 坑3：字符串常量锁

用字符串作为锁对象，比如 `synchronized("lock")`。字符串常量在常量池里是共享的，可能和其他地方的锁冲突。

**解法**：用 `private final Object lock = new Object();` 作为锁对象。

## 知识点总结

1. synchronized=卫生间门锁，一次进一个，内置锁，自动加解锁。

2. 三种用法：修饰实例方法（锁this）、修饰静态方法（锁Class）、修饰代码块（指定锁对象）。

3. 锁升级：无锁→偏向锁→轻量级锁→重量级锁，逐级升级不可逆。

4. 锁信息存在对象头里，锁的是对象不是代码。

5. 三大特性：可重入、非公平、不可中断。

6. 推荐用同步代码块，锁范围越小越好。

7. 一般场景用synchronized，需要高级功能才用ReentrantLock。
