# 并发协作工具笔记

## 一、什么是CountDownLatch

**定义** **CountDownLatch = 倒计时门闩**，是JUC包下的并发通信工具类，作用是让一个或多个线程等待其他线程完成前置任务后再继续执行。

CountDown是"计数递减"，Latch是"门闩"——门闩没打开前，谁都过不去。

打个比方：CountDownLatch就像**运动会的起跑发令枪**

- 有3名运动员要入场（3个前置任务），裁判在终点等着
- 每进来一个运动员，计数器减1
- 3个运动员都到齐了（计数器=0），"砰"一声枪响，裁判宣布比赛开始（等待的线程继续执行）
- 枪响之后就收不回来了——CountDownLatch只能用一次

## 二、CountDownLatch核心用法

### 核心方法

```
CountDownLatch latch = new CountDownLatch(3);  // 构造，设置初始计数

latch.await();             // 阻塞等待，直到计数减为0
latch.await(3, TimeUnit.SECONDS);  // 超时等待，超时后返回false
latch.countDown();         // 计数减1
latch.getCount();          // 获取当前计数值
```

### 完整示例：游戏加载资源

```java
public class CountDownLatchDemo {
    static class PreTask implements Runnable {
        private String task;
        private CountDownLatch latch;

        public PreTask(String task, CountDownLatch latch) {
            this.task = task;
            this.latch = latch;
        }

        @Override
        public void run() {
            try {
                Thread.sleep(new Random().nextInt(1000));
                System.out.println(task + " - 加载完成");
                latch.countDown();  // 完成一个，计数减1
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }

    public static void main(String[] args) throws Exception {
        CountDownLatch latch = new CountDownLatch(3);  // 3个前置任务

        // 主线程等待
        new Thread(() -> {
            System.out.println("等待资源加载...");
            latch.await();  // 阻塞，等3个任务都完成
            System.out.println("全部加载完成，游戏开始！");
        }).start();

        // 启动3个加载任务
        new Thread(new PreTask("加载地图", latch)).start();
        new Thread(new PreTask("加载人物", latch)).start();
        new Thread(new PreTask("加载音乐", latch)).start();
    }
}
```

### 实现原理

CountDownLatch内部基于AQS实现，有一个继承了AQS的内部类Sync：

- 构造时传入的count值，被设置到AQS的 `state` 变量中
- 调用 `countDown()` 时，通过CAS把state减1
- 调用 `await()` 时，判断state是否为0，不为0就进入AQS等待队列阻塞
- 当state减到0时，唤醒所有等待的线程

## 三、什么是生产者消费者模式

**定义** **生产者消费者模式**是经典的多线程并发协作模式：生产者线程生产数据放入共享区，消费者线程从共享区取数据消费，两者通过共享区解耦。

共享区满了生产者就阻塞等待，共享区空了消费者就阻塞等待。

打个比方：就像**快递驿站**

- 快递员（生产者）把包裹放到驿站货架（共享区）
- 取件人（消费者）从货架上取包裹
- 货架满了，快递员就先不送了，等着（生产者阻塞）
- 货架空了，取件人就等着，有新快递了再取（消费者阻塞）
- 快递员不关心谁来取，取件人不关心是谁送的——完全解耦

## 四、三种实现方式

### 方式1：Object的wait/notify

最原始的实现方式，利用对象监视器锁的等待通知机制。必须在synchronized块中调用。

```
// 标准范式：永远在while循环里wait
synchronized (共享对象) {
    while (条件不满足) {
        共享对象.wait();  // 释放锁，进入等待队列
    }
    // 条件满足，执行业务
}

// 通知方
synchronized (共享对象) {
    改变条件;
    共享对象.notifyAll();  // 唤醒所有等待线程
}
```

### 方式2：Lock的Condition

基于ReentrantLock的Condition，原理和wait/notify一样，只是API不同。

```
Lock lock = new ReentrantLock();
Condition condition = lock.newCondition();

// 等待方
lock.lock();
try {
    while (条件不满足) {
        condition.await();  // 对应wait()
    }
} finally {
    lock.unlock();
}

// 通知方
lock.lock();
try {
    改变条件;
    condition.signalAll();  // 对应notifyAll()
} finally {
    lock.unlock();
}
```

### 方式3：BlockingQueue（推荐）

最简单的实现方式，BlockingQueue自带阻塞的put和take方法，生产者消费者都只需要一行代码。

```java
static class Producer implements Runnable {
    private BlockingQueue<Integer> queue;
    public Producer(BlockingQueue<Integer> q) { this.queue = q; }

    @Override
    public void run() {
        while (true) {
            int data = new Random().nextInt();
            queue.put(data);  // 队列满了自动阻塞
        }
    }
}

static class Consumer implements Runnable {
    private BlockingQueue<Integer> queue;
    public Consumer(BlockingQueue<Integer> q) { this.queue = q; }

    @Override
    public void run() {
        while (true) {
            int data = queue.take();  // 队列空了自动阻塞
        }
    }
}

// 入口：5个生产者，10个消费者，队列容量8
BlockingQueue<Integer> queue = new LinkedBlockingQueue<>(8);
ExecutorService pool = Executors.newFixedThreadPool(15);
for (int i = 0; i < 5; i++)  pool.submit(new Producer(queue));
for (int i = 0; i < 10; i++) pool.submit(new Consumer(queue));
```

三种方式对比：BlockingQueue最简洁，实际开发优先用；wait/notify是底层原理，面试要懂；Condition用得较少。

## 五、关键技巧与坑点

### 技巧1：CountDownLatch配合实现并发测试

想让多个线程同时开始执行某个任务，可以用CountDownLatch做"发令枪"：

```
CountDownLatch startLatch = new CountDownLatch(1);  // 发令枪

for (int i = 0; i < 10; i++) {
    new Thread(() -> {
        startLatch.await();  // 所有线程在这里等
        // 真正的任务
    }).start();
}
startLatch.countDown();  // 一声令下，10个线程同时开始
```

### 技巧2：生产者消费者模式的四大优点

- **解耦**：生产者和消费者互不依赖，改一个不影响另一个
- **复用**：生产者类和消费者类可以独立复用
- **削峰**：短时间高并发请求可以先放进队列，慢慢消费，不会打垮系统
- **异步**：生产者不用等消费者处理完，放进去就返回，响应更快

### 坑点1：CountDownLatch只能用一次

**现象**：CountDownLatch减到0后，再调用await()不会再阻塞了。

**原因**：CountDownLatch没有重置计数的方法，一旦归零就失效了。

**解法**：需要重复使用的场景，用CyclicBarrier（可循环使用的屏障）。

### 坑点2：countDown一定要放在finally里

**现象**：程序卡死，所有线程都不动了。

**原因**：某个前置任务抛异常了，没执行到countDown，计数器永远到不了0，等待线程一直阻塞。

**解法**：把countDown放在finally块中，确保一定会执行。

### 坑点3：wait必须在while循环里，不能用if

**现象**：用if判断条件+wait，多线程下偶尔出异常，比如数组越界。

**原因**：线程被唤醒后条件可能已经变了。比如两个消费者，一个消费完了数据，另一个被唤醒后数据已经空了，用if的话不会再判断就直接操作。

**解法**：永远在while循环中调用wait，唤醒后重新检查条件，不满足就继续等。这是黄金法则第一条。

### 坑点4：用notifyAll不用notify

**现象**：多生产者多消费者下，程序突然"假死"，所有线程都在等。

**原因**：notify只随机唤醒一个线程，如果唤醒的是同类线程（比如生产者唤醒生产者），那消费者那边永远没人唤醒，最后大家都等着。

**解法**：用notifyAll而不是notify，用signalAll而不是signal。这是黄金法则第二条。

### 坑点5：notify早期通知导致永久阻塞

**现象**：线程A还没调用wait，线程B已经notify了，导致A之后调用wait时没人唤醒，永远阻塞。

**原因**：notify不留下"通知记录"，如果等待的线程还没开始等，通知就丢失了。

**解法**：配合一个boolean状态变量，notify前改变状态，wait前先判断状态，如果已经满足就不用等了。

## 六、总结

- CountDownLatch = 倒计时门闩，让一个线程等多个前置任务完成后再执行，只能用一次
- CountDownLatch基于AQS实现，state存计数值，countDown减1，await阻塞等待
- 生产者消费者模式通过共享队列解耦生产者和消费者，是最经典的并发协作模式
- 三种实现方式：wait/notify（底层）、Condition（Lock版）、BlockingQueue（推荐，最简洁）
- 生产者消费者模式四大优点：解耦、复用、削峰、异步
- 两条黄金法则：①永远在while循环中wait；②用notifyAll不用notify
- CountDownLatch的countDown一定要放在finally里，确保异常时也能执行
- 需要可重复使用的屏障，用CyclicBarrier而不是CountDownLatch
